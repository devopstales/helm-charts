apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "trivy-image-validator.fullname" . }}-test-connection"
  labels:
    {{- include "trivy-image-validator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "trivy-image-validator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
